#!/bin/sh

if [ -n "$GCS_UPLOADS_BUCKET" ]; then
  nginx=$(id -u nginx)
  gcsfuse -o allow_other --log-file /var/log/gcsfuse --uid $nginx --implicit-dirs --only-dir app/uploads $GCS_UPLOADS_BUCKET web/app/uploads
fi

exec "$@"
